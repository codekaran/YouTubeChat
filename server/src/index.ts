import { ChatServer } from './chat-server';

let app = new ChatServer().getApp();
export { app };
// import { IHeroModel,HeroSchema, HeroModel, IRead, IWrite,RepositoryBase, HeroRepository } from './hero';

// let app = new ChatServer().getApp();
// export { app };